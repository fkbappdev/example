import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderRadius: 50,
    borderWidth: 2,
    borderColor: "red",
  },
  image: {
    borderRadius: 50,
    borderWidth: 1,
    borderColor: "white",
  }
});

export default styles;
